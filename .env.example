# Server
PORT=3000
NODE_ENV=development

# x402 micropayments (disabled by default — set X402_ENABLED=true to activate)
# X402_ENABLED=true
# WALLET_ADDRESS=0xYourWalletAddress
# FACILITATOR_URL=https://facilitator.x402.org

# CORS (production: restrict to frontend domain)
# CORS_ORIGIN=https://ibanforge.com

# Admin API (for managing API keys remotely)
# ADMIN_SECRET=your-secret-here

# Stats / admin endpoints (Bearer token required for /stats and /admin/*)
# STATS_TOKEN=your-long-random-token-here

# Basescan API key for /admin/revenue (free at https://basescan.org/myapikey)
# Optional — works without a key but rate-limited to 1 req/sec.
# BASESCAN_API_KEY=

# Optional: override database paths
# BIC_DB_PATH=./data/bic.sqlite
# STATS_DB_PATH=./data/stats.sqlite
