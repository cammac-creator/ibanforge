"""Single source of truth for the package version.

Imported by __init__ (as __version__) and by both clients (to build the
User-Agent), so a version bump only needs to happen here.
"""

__version__ = "1.3.2"
