---
title: "Cles API"
description: "Generez une cle API gratuite pour obtenir 200 requetes/mois sur tous les endpoints IBANforge — sans carte bancaire, sans portefeuille crypto."
order: 1
---

# Cles API

IBANforge propose une **offre gratuite** via des cles API — 200 requetes/mois sur tous les endpoints. Aucune carte bancaire, aucun portefeuille crypto requis. Pour des volumes plus eleves, vous pouvez utiliser les [micropaiements x402](/docs/x402).

## Generer une cle API gratuite

Envoyez une requete POST a `/v1/keys/generate` avec votre adresse email :

```bash
curl -X POST https://api.ibanforge.com/v1/keys/generate \
  -H "Content-Type: application/json" \
  -d '{"email": "vous@exemple.com"}'
```

### Reponse

```json
{
  "key": "ifk_live_xxxxxxxxxxxxxxxxxxxx",
  "email": "vous@exemple.com",
  "tier": "free",
  "quota": {
    "requests_per_month": 200,
    "used": 0,
    "resets_at": "2026-05-01T00:00:00.000Z"
  }
}
```

Votre cle commence par `ifk_` (IBANforge Key). **Conservez-la en securite** — elle ne sera plus affichee.

## Utiliser votre cle API

Transmettez la cle dans l'en-tete `Authorization` en tant que token Bearer :

```bash
curl -X POST https://api.ibanforge.com/v1/iban/validate \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ifk_live_xxxxxxxxxxxxxxxxxxxx" \
  -d '{"iban": "CH10 0023 0000 0000 1234 5"}'
```

La cle fonctionne sur tous les endpoints payants :
- `POST /v1/iban/validate`
- `POST /v1/iban/batch`
- `GET /v1/bic/:code`
- `POST /v1/iban/compliance`
- `GET /v1/ch/clearing/:iid`

## Les offres en un coup d'oeil

| Plan | Volume | Cout |
|---|---|---|
| Gratuit (cle API) | 200 requetes/mois | 0 $ |
| Packs de credits prepayes — carte ou USDC | 1k / 5k / 25k appels | 5 $ / 20 $ / 80 $ — n'expirent jamais |
| x402 paiement a l'appel | Illimite | 0,002 $–0,02 $/appel |

Les 200 requetes gratuites sont partagees entre tous les endpoints et se reinitalisent le 1er de chaque mois.

## Verifier votre consommation

```bash
curl https://api.ibanforge.com/v1/keys/usage \
  -H "Authorization: Bearer ifk_live_xxxxxxxxxxxxxxxxxxxx"
```

### Reponse

```json
{
  "tier": "free",
  "quota": {
    "requests_per_month": 200,
    "used": 47,
    "remaining": 153,
    "resets_at": "2026-05-01T00:00:00.000Z"
  }
}
```

## Lorsque votre quota est depasse

Votre intégration ne tombe **pas** sur un cul-de-sac. Une fois les 200 requêtes mensuelles épuisées, l'API répond `402 Payment Required` (x402) plutôt qu'un 429 sec, avec des en-têtes qui disent exactement ce qui s'est passé :

```
X-Quota-Exhausted: true
X-Quota-Used: 200
X-Quota-Limit: 200
X-Quota-Month: 2026-07
```

Le corps du 402 liste vos trois options, lisibles par machine :

1. **Acheter un pack de crédits prépayés** — par carte sur la page [tarifs](/pricing), ou en USDC via `POST /v1/credits/buy/1k|5k|25k`. Les crédits n'expirent jamais et s'attachent à votre clé existante.
2. **Payer à l'appel via x402** — un client compatible x402 paie automatiquement en USDC, sans changer de clé. Voir [Paiements x402](/docs/x402).
3. **Attendre la réinitialisation mensuelle** — le quota se réinitialise le 1er de chaque mois.

## Exemple TypeScript

```typescript
const API_KEY = process.env.IBANFORGE_API_KEY;

const response = await fetch("https://api.ibanforge.com/v1/iban/validate", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": `Bearer ${API_KEY}`,
  },
  body: JSON.stringify({ iban: "CH10 0023 0000 0000 1234 5" }),
});

const data = await response.json();
console.log(data);
```

## Exemple Python

```python
import os
import requests

API_KEY = os.environ["IBANFORGE_API_KEY"]

response = requests.post(
    "https://api.ibanforge.com/v1/iban/validate",
    headers={
        "Content-Type": "application/json",
        "Authorization": f"Bearer {API_KEY}",
    },
    json={"iban": "CH10 0023 0000 0000 1234 5"},
)

data = response.json()
print(data)
```

## Etapes suivantes

- [Micropaiements x402](/docs/x402) — paiement a l'appel illimite avec USDC
- [Validation IBAN](/docs/iban-validate) — reference complete de l'endpoint
- [Reference des erreurs](/docs/errors) — tous les codes d'erreur et le depannage
